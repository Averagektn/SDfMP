<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:FilmsBrowser.ViewModels" x:DataType="viewmodels:RegistrationViewModel"
             x:Class="FilmsBrowser.Views.RegistrationPage">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Entry Placeholder="Login" Text="{Binding Login}" Keyboard="Text" />
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />
            <Button VerticalOptions="Center" Text="Sign Up" Command="{Binding RegistrationCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>